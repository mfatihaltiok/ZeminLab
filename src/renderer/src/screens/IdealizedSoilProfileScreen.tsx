import { useState } from 'react'
import { Frame, Card, Metric, Table, Source } from '../workspace/WorkspaceShell'
import type { BoreholeRecord, LaboratoryRecord } from '../../core/models/field-data'
import type { IdealizedSoilLayer, IdealizedSoilProfile } from '../../core/models/idealized-soil-profile'

function build(boreholes:BoreholeRecord[],labs:LaboratoryRecord[],count:number):IdealizedSoilLayer[]{
 if(!boreholes.length)return[]; const max=Math.max(...boreholes.map(b=>b.totalDepth),1); const cuts=new Set<number>([0,max])
 boreholes.forEach(b=>b.lithology.forEach(x=>{if(x.from>0&&x.from<max)cuts.add(x.from);if(x.to>0&&x.to<max)cuts.add(x.to)}))
 let a=[...cuts].sort((x,y)=>x-y); if(a.length-1!==count){a=[0,...Array.from({length:count-1},(_,i)=>Math.round((max*(i+1)/count)*100)/100),max].sort((x,y)=>x-y)}
 return a.slice(0,-1).map((top,i)=>{const bottom=a[i+1];const bs=boreholes.filter(b=>b.lithology.some(x=>x.to>top&&x.from<bottom));const ss=boreholes.flatMap(b=>b.spt.filter(s=>s.depth>=top&&s.depth<bottom).map(s=>({...s,boreholeId:b.id})));const ls=labs.filter(l=>l.depth>=top&&l.depth<bottom);const li=bs.flatMap(b=>b.lithology).find(x=>x.from<bottom&&x.to>top);const la=ls[0];const ns=ss.map(s=>s.n2!=null&&s.n3!=null?s.n2+s.n3:undefined).filter((n):n is number=>n!=null);return{id:crypto.randomUUID(),order:i+1,topDepth:top,bottomDepth:bottom,soilName:li?.description||la?.soilDescription||'Tanımlanmamış zemin',soilCode:li?.code||la?.soilCode||'',boreholeIds:bs.map(b=>b.id),sptRecordIds:ss.map(s=>s.id),laboratoryRecordIds:ls.map(l=>l.id),representativeSptN:ns.length?ns.reduce((x,n)=>x+n,0)/ns.length:undefined,gamma:li?.unitWeight??la?.unitWeight,gammaSat:li?.saturatedUnitWeight,waterContent:la?.waterContent,liquidLimit:li?.liquidLimit??la?.liquidLimit,plasticLimit:li?.plasticLimit??la?.plasticLimit,plasticityIndex:li?.plasticityIndex??la?.plasticityIndex,finesContent:li?.finesContent??la?.finesContent,cohesion:li?.cohesion??la?.c,frictionAngle:li?.frictionAngle??la?.phi,compressionIndexCc:la?.consolidationCc,recompressionIndexCr:la?.consolidationCs,constrainedModulus:la?.elasticModulus,oedometricModulus:la?.elasticModulus,poissonRatio:la?.poissonRatio,parameterSources:{},userOverride:false}})
}
function fresh(bs:BoreholeRecord[],ls:LaboratoryRecord[],count:number,p?:IdealizedSoilProfile):IdealizedSoilProfile{return{id:p?.id??crypto.randomUUID(),version:p?.version??1,status:'TASLAK',targetLayerCount:count,generatedAt:new Date().toISOString(),sourceBoreholeIds:bs.map(b=>b.id),sourceLaboratoryIds:ls.map(l=>l.id),layers:build(bs,ls,count),methodology:'TBDY 2018 + yürürlükteki Türk mevzuatı + ilgili TS/TS EN/TS EN ISO standartları. Otomatik katmanlama başlangıç mühendislik modelidir; nihai profil kullanıcı kontrolünden sonra sabitlenir.'}}
export function IdealizedSoilProfileScreen({boreholes,labs,profile,onChange}:{boreholes:BoreholeRecord[];labs:LaboratoryRecord[];profile?:IdealizedSoilProfile;onChange:(p:IdealizedSoilProfile)=>void}){
 const [sel,setSel]=useState(0);const [count,setCount]=useState(profile?.targetLayerCount??3);const p=profile??fresh(boreholes,labs,count);const l=p.layers[sel];
 const edit=(patch:Partial<IdealizedSoilLayer>)=>{if(!l||p.status==='SABİTLENDİ')return;onChange({...p,layers:p.layers.map((x,i)=>i===sel?{...x,...patch,userOverride:true}:x)})};
 if(!boreholes.length)return <Frame screen="profile"><div className="empty-state"><strong>İdealize zemin profili için önce sondaj ve SPT verisi girilmelidir.</strong></div></Frame>;
 return <Frame screen="profile"><div className="idealized-toolbar"><div><b>İDEALİZE ZEMİN PROFİLİ</b><span>Oturma hesabında kullanılacak mühendislik modeli.</span></div><div className="idealized-actions"><label>Hedef katman <input type="number" min="1" max="20" value={count} disabled={p.status==='SABİTLENDİ'} onChange={e=>setCount(Math.max(1,Math.min(20,Number(e.target.value))))}/></label><button disabled={p.status==='SABİTLENDİ'} onClick={()=>onChange(fresh(boreholes,labs,count,p))}>Otomatik Oluştur</button><button className="primary-button" disabled={p.status==='SABİTLENDİ'} onClick={()=>onChange({...p,status:'SABİTLENDİ',frozenAt:new Date().toISOString(),version:p.version+1})}>Profili Sabitle</button></div></div><Source><b>Kaynak:</b> TBDY 2018 → Türk mevzuatı → ilgili TS/TS EN/TS EN ISO. Verisi olmayan parametreler varsayılmaz.</Source><div className="metric-strip"><Metric label="Durum" value={p.status}/><Metric label="Katman" value={p.layers.length}/><Metric label="Sondaj" value={p.sourceBoreholeIds.length}/><Metric label="SPT" value={p.layers.reduce((n,x)=>n+x.sptRecordIds.length,0)}/><Metric label="Laboratuvar" value={p.layers.reduce((n,x)=>n+x.laboratoryRecordIds.length,0)}/><Metric label="Versiyon" value={p.version}/></div><div className="idealized-layout"><Card title="İDEALİZE KATMANLAR"><table className="data-table"><thead><tr>{['#','Üst','Alt','Kalınlık','Zemin','N-SPT','γ','c','φ'].map(x=><th key={x}>{x}</th>)}</tr></thead><tbody>{p.layers.map((x,i)=><tr key={x.id} onClick={()=>setSel(i)} className={i===sel?'idealized-selected':''}><td>{i+1}</td><td>{x.topDepth.toFixed(2)}</td><td>{x.bottomDepth.toFixed(2)}</td><td>{(x.bottomDepth-x.topDepth).toFixed(2)}</td><td>{x.soilName}</td><td>{x.representativeSptN?.toFixed(1)??'—'}</td><td>{x.gamma?.toFixed(2)??'—'}</td><td>{x.cohesion?.toFixed(2)??'—'}</td><td>{x.frictionAngle?.toFixed(1)??'—'}</td></tr>)}</tbody></table></Card><aside className="idealized-properties"><div className="calculation-card-title">ÖZELLİKLER · {l?`KATMAN ${l.order}`:'—'}</div>{l&&<div className="idealized-form"><label>Zemin adı<input value={l.soilName} disabled={p.status==='SABİTLENDİ'} onChange={e=>edit({soilName:e.target.value})}/></label><label>Sınıflandırma<input value={l.soilCode} disabled={p.status==='SABİTLENDİ'} onChange={e=>edit({soilCode:e.target.value})}/></label><label>Üst (m)<input type="number" value={l.topDepth} disabled={p.status==='SABİTLENDİ'} onChange={e=>edit({topDepth:Number(e.target.value)})}/></label><label>Alt (m)<input type="number" value={l.bottomDepth} disabled={p.status==='SABİLENDİ'} onChange={e=>edit({bottomDepth:Number(e.target.value)})}/></label><label>γ<input type="number" value={l.gamma??''} disabled={p.status==='SABİLENDİ'} onChange={e=>edit({gamma:Number(e.target.value)})}/></label><label>γsat<input type="number" value={l.gammaSat??''} disabled={p.status==='SABİLENDİ'} onChange={e=>edit({gammaSat:Number(e.target.value)})}/></label><label>c<input type="number" value={l.cohesion??''} disabled={p.status==='SABİLENDİ'} onChange={e=>edit({cohesion:Number(e.target.value)})}/></label><label>φ (°)<input type="number" value={l.frictionAngle??''} disabled={p.status==='SABİLENDİ'} onChange={e=>edit({frictionAngle:Number(e.target.value)})}/></label><label>CC<input type="number" value={l.compressionIndexCc??''} disabled={p.status==='SABİLENDİ'} onChange={e=>edit({compressionIndexCc:Number(e.target.value)})}/></label><label>CR<input type="number" value={l.recompressionIndexCr??''} disabled={p.status==='SABİLENDİ'} onChange={e=>edit({recompressionIndexCr:Number(e.target.value)})}/></label><div className="property-source">SPT: {l.sptRecordIds.length} · Laboratuvar: {l.laboratoryRecordIds.length} · Sondaj: {l.boreholeIds.join(', ')||'—'}</div></div>}</aside></div></Frame>
}
