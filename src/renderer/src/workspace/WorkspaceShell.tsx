import {useState,type ReactNode} from 'react'
import {useProjectInfo} from '../../../core/state/project-store'

export type ScreenId='dashboard'|'project-info'|'field'|'profile'|'earthquake'|'bearing-capacity'|'settlement'|'liquefaction'|'foundation'|'jet-grout'|'report'
export const SCREEN_META:Record<ScreenId,{title:string;group:string}>={dashboard:{title:'Proje Özeti',group:'PROJE'},'project-info':{title:'Proje Bilgileri',group:'PROJE'},field:{title:'Saha / Sondaj',group:'SAHA'},profile:{title:'Zemin Profili',group:'SAHA'},earthquake:{title:'Deprem Parametreleri',group:'ANALİZ'},'bearing-capacity':{title:'Taşıma Gücü',group:'ANALİZ'},settlement:{title:'Oturma',group:'ANALİZ'},liquefaction:{title:'Sıvılaşma',group:'ANALİZ'},foundation:{title:'Temel Tasarımı',group:'TASARIM'},'jet-grout':{title:'Jet Grout',group:'TASARIM'},report:{title:'Mühendislik Raporu',group:'RAPOR'}}
export function Frame({screen,children}:{screen:ScreenId;children:ReactNode}){const m=SCREEN_META[screen];return <section className="engineering-screen"><header className="engineering-header"><div><div className="engineering-header-category">{m.group}</div><h2>{m.title}</h2></div></header><div className="engineering-content">{children}</div></section>}
export function Field({label,value,onChange,type='number'}:{label:string;value:string|number;onChange:(v:string)=>void;type?:string}){return <label><span>{label}</span><input type={type} value={value} onChange={e=>onChange(e.target.value)}/></label>}
export function Card({title,children}:{title:string;children:ReactNode}){return <div className="calculation-card"><div className="calculation-card-title">{title}</div>{children}</div>}
export function Metric({label,value,unit,tone}:{label:string;value:string|number;unit?:string;tone?:string}){return <div className={`metric ${tone??''}`}><span>{label}</span><strong>{value}</strong>{unit&&<small>{unit}</small>}</div>}
export function Source({children}:{children:ReactNode}){return <div className="engineering-note"><b>Kaynak / yöntem:</b> {children}</div>}
export function Table({headers,rows}:{headers:string[];rows:(string|number)[][]}){return <table className="data-table"><thead><tr>{headers.map(h=><th key={h}>{h}</th>)}</tr></thead><tbody>{rows.map((r,i)=><tr key={i}>{r.map((v,j)=><td key={j}>{v}</td>)}</tr>)}</tbody></table>}
export function WorkspaceShell({screen,onScreenChange,children}:{screen:ScreenId;onScreenChange:(screen:ScreenId)=>void;children:ReactNode}){const project=useProjectInfo();const [expanded,setExpanded]=useState<Record<string,boolean>>({PROJE:true,SAHA:true,ANALİZ:true,TASARIM:true,RAPOR:true});const go=(id:ScreenId)=>onScreenChange(id);const groups=[{name:'PROJE',items:['dashboard','project-info'] as ScreenId[]},{name:'SAHA',items:['field','profile'] as ScreenId[]},{name:'ANALİZ',items:['earthquake','bearing-capacity','settlement','liquefaction'] as ScreenId[]},{name:'TASARIM',items:['foundation','jet-grout'] as ScreenId[]},{name:'RAPOR',items:['report'] as ScreenId[]}];return <div className="app-shell"><div className="title-bar"><div className="app-title"><div className="app-mark">Z</div><b>ZeminLab</b><span className="title-separator">|</span><span className="project-name">{project.title||'Yeni Proje'}</span></div><div className="window-controls"><button>_</button><button>□</button><button className="close">×</button></div></div><div className="menu-bar">{['Dosya','Düzen','Görünüm','Tanımlar','Analiz','Tasarım','Rapor','Yardım'].map(x=><button key={x}>{x}</button>)}</div><div className="toolbar"><div className="toolbar-section">{[['＋','Yeni','dashboard'],['▣','Aç','project-info'],['▤','Kaydet','report']].map(([icon,label,id])=><button className="tool-button" key={label} onClick={()=>go(id as ScreenId)}><span>{icon}</span><b>{label}</b></button>)}</div><div className="toolbar-divider"/><div className="toolbar-section">{[['⌕','Sondaj','field'],['N','SPT','field'],['▥','Laboratuvar','field']].map(([icon,label,id])=><button className="tool-button" key={label} onClick={()=>go(id as ScreenId)}><span>{icon}</span><b>{label}</b></button>)}</div><div className="toolbar-divider"/><div className="toolbar-section">{[['∑','Analiz','bearing-capacity'],['⌂','Temel','foundation'],['◉','Jet Grout','jet-grout'],['▤','Rapor','report']].map(([icon,label,id])=><button className="tool-button emphasis" key={label} onClick={()=>go(id as ScreenId)}><span>{icon}</span><b>{label}</b></button>)}</div><div className="toolbar-spacer"/><span className="units-label">Birim</span><select className="units-select" defaultValue={project.unitSystem}><option>kN-m</option><option>ton-m</option><option>kPa-m</option></select></div><div className="main-layout"><aside className="panel"><div className="panel-header"><span>MODEL EXPLORER</span><span>◫</span></div><div className="project-header"><b>▾</b><span>{project.projectNo||'YENİ PROJE'}</span></div><div className="tree">{groups.map(group=><div key={group.name}><div className="tree-row clickable" onClick={()=>setExpanded(s=>({...s,[group.name]:!s[group.name]}))}><span className={`tree-arrow ${expanded[group.name]?'expanded':''}`}>▶</span><span className="tree-icon">▣</span><b>{group.name}</b></div>{expanded[group.name]&&<div className="tree-children">{group.items.map(id=><div key={id} className={`tree-row clickable ${screen===id?'active':''}`} onClick={()=>go(id)}><span className="tree-arrow"/><span className="tree-icon">•</span><span>{SCREEN_META[id].title}</span></div>)}</div>}</div>)}</div></aside><main className="workspace"><div className="workspace-tabs"><button className="workspace-tab selected">{SCREEN_META[screen].title}<span className="tab-close">×</span></button><div className="tab-spacer"/><button className="workspace-tab-action">+</button></div><div className="workspace-content">{children}</div></main><aside className="panel"><div className="panel-header"><span>PROPERTIES</span><span>×</span></div><div className="form-section"><div className="section-title">AKTİF PROJE</div><div className="form-grid" style={{gridTemplateColumns:'1fr'}}><Field label="Proje" type="text" value={project.title} onChange={()=>{}}/><Field label="Proje No" type="text" value={project.projectNo} onChange={()=>{}}/><Field label="Mühendis" type="text" value={project.engineer} onChange={()=>{}}/></div></div></aside></div><div className="status-bar"><span>{SCREEN_META[screen].group} / {SCREEN_META[screen].title}</span><span className="status-center">ZeminLab Engineering Workspace</span><span className="status-right"><span><i className="status-indicator"/>Hazır</span><span>{project.unitSystem}</span></span></div></div>}
